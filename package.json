{
  "name": "exif-sidecar",
  "version": "0.1.0",
  "description": "Convert JPEG EXIF metadata to and from a plain JSON sidecar format",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "node --test test/"
  },
  "dependencies": {}
}
